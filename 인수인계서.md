# 모두의 내일 (Naeil) 프로젝트 인수인계서

작성일: 2026-08-25
프로젝트: 모두의 내일 (접근성 진단 시스템)

## 1. 프로젝트 개요 및 기술 스택
본 프로젝트는 특정 구역 및 시설의 무장애(접근성) 진단 데이터를 시각화하고 관리하는 대시보드 웹 애플리케이션입니다.

- **Framework**: Next.js 16 (App Router)
- **Language**: TypeScript
- **Styling**: Tailwind CSS
- **Map API**: Kakao Maps API (`react-kakao-maps-sdk`)
- **Charting**: Recharts
- **Database/Backend**: Supabase (PostgreSQL)
- **Geospatial Processing**: Turf.js (다각형 내부 좌표 판별 등)
- **Icons**: Lucide React

## 2. 주요 폴더 구조 및 역할
- `/src/app`: Next.js 라우팅 및 메인 페이지(`page.tsx`) 위치.
- `/src/components`: UI 컴포넌트 모음. (지도, 사이드바, 차트 등)
- `/src/contexts`: 전역 상태 및 인증(Role) 관리를 위한 `AuthContext.tsx`.
- `/src/data`: DB 연결 실패 시 사용되는 Mock Data (`mock.json`).
- `/src/constants`: 공통 컬러셋(`colors.ts`), 평가 기준(`standards.ts`) 등 상수 모음.
- `/src/utils`: Supabase 클라이언트(`supabase.ts`), 지리 데이터 유틸리티(`geo.ts`).
- `/supabase`: DB 스키마 파일(`supabase_schema.sql`).

## 3. 구현된 핵심 기능 (A to Z)

### A. 데이터 연동 및 상태 관리 (Supabase & Mock)
- Supabase의 `zones`, `sub_zones`, `facilities`, `category_scores` 테이블과 실시간으로 연동.
- DB 연결 실패나 데이터가 없을 경우 `mock.json` 기반으로 안전하게 Fallback 되도록 설계.

### B. 권한 기반 데이터 필터링 (RBAC)
- `AuthContext`를 통해 유저 Role(`admin`, `official`, `viewer`)을 구분.
- **Admin**: 모든 데이터(비공개 포함) 및 하위 구역 관리 기능 접근 가능.
- **Official (담당자)**: 본인에게 할당된 구역(`assignedZoneId`)의 모든 데이터 및 타 구역의 공개 데이터 열람.
- **Viewer**: 공개(Public) 상태의 데이터만 열람.

### C. 인터랙티브 지도 (KakaoMap.tsx)
- 카카오맵 API를 활용하여 구역(Zone)과 하위 구역(Sub-zone)을 폴리곤(다각형) 형태로 렌더링.
- 시설 위치에 따른 마커 표시 및 점수에 따른 색상(Color Grade) 시각화.
- **그리기 모드 (Drawing Mode)**: 지도 위를 클릭하여 다각형의 꼭짓점을 생성해 새로운 하위 구역을 그릴 수 있는 인터랙션 구현.

### D. 사이드바 및 구역 관리 (Sidebar.tsx)
- 선택된 구역의 상세 정보를 표시하며, 하위 구역(Sub-zones) CRUD (생성, 편집, 삭제) 기능 포함.
- 지도에서 그린 다각형(Polygon)을 기반으로 해당 영역 내부에 속한 시설(`isPointInPolygon` 활용)을 자동 식별하고 평균 접근성 점수를 산출하여 하위 구역을 등록.

### E. 카테고리별 맞춤형 데이터 시각화 (Recharts)
- **종합 균형 (Radar Chart)**: 5대 카테고리(보행로, 출입구, 화장실, 엘리베이터, 주차장)의 점수 밸런스 시각화.
- **보행로 (Area Chart)**: 접근성 구간별 흐름 표시.
- **출입구 (Bar Chart)**: 출입구별 접근성 점수 비교.
- **화장실 & 주차장 (Pie / Donut Chart)**: 우수/보통/미흡 등급별 분포 비율.
- **엘리베이터 (Line Chart)**: 접근성 현황의 흐름선.
- 카테고리 클릭 시 해당 카테고리 점수 기준으로 시설 순위가 즉시 정렬되도록 연동.

### F. 시설 상세 진단 정보 (FacilityDetail.tsx)
- 특정 시설 클릭 시 세부 측정치(JSON 형태의 실측 수치 및 특이사항) 파싱 후 표시.
- 현장 사진(`image_url`) 렌더링 및 에러 뷰 처리.

### G. PDF 보고서 인쇄 기능
- Tailwind의 `print:` 모디파이어를 적극 활용(`print:hidden`, `print:block`, `page-break-inside-avoid`).
- 화면에서는 사이드바 위주로 작동하나, 인쇄(`Ctrl+P` 또는 버튼 클릭) 시 불필요한 UI는 숨기고 지도/차트/상세 데이터가 보고서 포맷으로 출력되도록 구성.

## 4. 향후 작업 및 유지보수 참고사항
1. **Kakao Map API Key**: `.env.local`의 `NEXT_PUBLIC_KAKAO_MAP_KEY`가 올바르게 설정되어야 지도가 표시됩니다. (현재 설정 안 되어있으면 Placeholder UI 렌더링됨)
2. **Supabase 설정**: `.env.local`에 Supabase URL과 Anon Key가 연결되어 있어야 DB 실시간 연동이 적용됩니다. 스키마 변경 시 `supabase_schema.sql`을 참조하세요.
3. **추가 확장 포인트**: 구역(Zone)과 시설(Facility)의 추가/수정/삭제 폼은 아직 UI에 전부 노출되어 있지 않고(하위 구역만 관리 가능), Admin 전용 별도 대시보드(`/app/admin`) 페이지 확장이 필요할 수 있습니다.
