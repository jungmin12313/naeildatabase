# 구역별 무장애지도 점수 분석 작업 지시서 (재사용 프롬프트)

아래 절차에 따라 세 가지 입력 자료를 분석하고, 구역별 최종 넓이 지수를 산출하라.

## 입력 자료 (3종, 파일명은 매번 다를 수 있음)

1. **구역 좌표 마킹 시트** — `구역명, GPS` 형태의 CSV. 하나의 GPS 좌표가 여러 구역에 동시에 속할 수 있다(예: `"1 , 2 , 3"`).
2. **n대 카테고리 점수화 공식** — PDF. S1, S2, S3 … 각 카테고리별 세부 점수 산출식과, 이를 합산하는 "최종 넓이 지수(Final/Result Index)" 공식이 들어있다. 카테고리 개수는 5개(S1~S5)로 고정되어 있지 않을 수 있다.
3. **현장답사 데이터 시트** — CSV. 각 행은 하나의 실측 지점이며 `카테고리` 컬럼(보행로/출입구/화장실/엘리베이터/주차장 등)과 그에 대응하는 실측값(유효폭, 단차, 기울기, 문너비, 가로/세로너비 등) 컬럼을 가진다.

## 처리 절차

### 1단계 — 구역 도형 구성
- 좌표 마킹 시트를 파싱하여, 구역 번호별로 "그 번호를 포함하는 모든 좌표점"을 모은다.
- 각 구역의 점 집합으로 **convex hull**을 만들어 폴리곤으로 정의한다. (격자가 매끈한 사각형이 아니거나 점이 3개뿐이어도 그대로 진행하되, 도형 형태가 직관과 다르면—예: 특정 구역이 비정상적으로 넓어짐— 반드시 그 사실을 사용자에게 보고한다.)
- 구역들은 서로 겹칠 수 있다. 겹침 자체를 오류로 간주하지 말고 그대로 반영한다.

### 2단계 — 점수화 공식 파싱 (절대 임의 가정 금지)
- PDF에서 **존재하는 모든 카테고리(S1, S2, S3, …, Sn)** 의 세부 산출식을 추출한다. S1~S5로 개수를 고정하지 말고, PDF에 실제로 몇 개의 카테고리가 정의되어 있는지 먼저 확인한다.
- 각 카테고리 공식의 조건부 분기(예: 단차 ≤2cm → 100점 만점, 그 외 50:50 합산 등), clamp 범위(0~100), 가중치를 빠짐없이 반영한다.
- **최종 넓이 지수 공식도 PDF에서 그대로 가져온다.** 현재 알려진 형태(S1~S5, 5개 기준)는 다음과 같은 순환 구조다.

  ```
  Final Index = ( S1·S2 + S2·S3 + S3·S4 + S4·S5 + S5·S1 ) / (5 × 10000) × 100
  ```

  - 만약 카테고리가 6개 이상(S6 등)으로 늘어난 경우, **PDF에 갱신된 공식이 있는지 먼저 확인**한다.
  - PDF에 n개 카테고리에 대한 공식이 명시되어 있지 않다면, 위 구조를 인접 항끼리 순환 곱하는 일반형으로 확장한다:
    ```
    Final Index = ( Σ_{i=1..n} S_i · S_(i+1 mod n) ) / (n × 10000) × 100
    ```
    단, 이는 어디까지나 PDF에 명시된 공식이 없을 때의 대체 방식이므로, 이렇게 확장했다는 사실과 근거를 결과물에 반드시 명시한다.

- **경사로 미설치 예외 처리:** S2, S3, S4 카테고리에서 단차가 2cm를 초과함에도 경사로가 설치되지 않았거나 기울기 실측값이 누락(NaN/Null)된 경우, 경사로 점수(s_slope)는 0점으로 처리한다.
- **세부 점수 클램핑(Clamping):** 모든 세부 조사 항목별 점수(s_1, s_2, s_width, s_step, s_slope 등)는 계산 직후 즉시 0~100 범위로 제한(min 100, max 0)한 후 가중치 합산을 수행한다.
- **문서 내 표기 오류 정정:** 
  - S2 세부/최종 점수 범위: 0 이상 100 이하 (0 <= score <= 100)
  - S5 세로 너비 0점 기준: 실측값 166.67cm (500/3 cm) 이하일 때 0점

### 3단계 — 실측 데이터 ↔ 카테고리 매핑
- 현장답사 시트의 `카테고리` 컬럼값(보행로/출입구/화장실/엘리베이터/주차장 등)을 PDF에 정의된 S1~Sn 각각의 이름과 매칭한다.
- 각 카테고리 공식이 요구하는 변수(예: S2의 x1=유효폭)와 실측 데이터의 실제 컬럼명이 다를 경우(예: 출입구 데이터엔 "유효폭" 컬럼이 비어 있고 "문너비" 컬럼에 값이 있음), 값의 스케일과 의미를 근거로 합리적 매핑을 하되 **매핑 근거를 결과물에 명시**한다.
- **이상치 정정 (전처리):** 문너비 등 실측값이 명백한 오기입(예: 98.80m, 8.33m, 8.11m)으로 보이는 경우, 이를 결측(N/A) 처리하여 집계에서 제외하지 말고, 실제 의도된 값(0.98m, 0.83m, 0.81m 등)으로 변환하여 정상 계산에 포함시킨다.
- 필요한 필드가 비어 있는 행은 해당 카테고리 점수 계산에서 제외하고, 제외된 개수를 보고한다.

### 4단계 — 개별 데이터 포인트 점수 산출 (S1~Sn 전부, 임의 대입 금지)
- **이전 작업과 달리, 데이터가 존재하는 모든 카테고리에 대해 실제 공식으로 점수를 계산한다.** "다른 카테고리는 100점으로 가정" 같은 임의 대입은 사용자가 명시적으로 지시하지 않는 한 절대 하지 않는다.
- 각 실측 지점에 대해 해당 카테고리의 공식을 그대로 적용해 Sn 값을 계산한다(중간 세부점수 s1, s2, s3 … 포함).

### 5단계 — 구역 소속 판정
- 각 실측 지점의 GPS가 어느 구역 폴리곤 내부(경계 포함)에 있는지 판정한다. 한 지점이 여러 구역에 동시에 속할 수 있으며, 그 경우 해당하는 모든 구역의 집계에 포함시킨다.

### 6단계 — 구역별 카테고리 점수 집계 및 결측치 대체 (전체 평균 대입)
1. **단일 카테고리 대표점수 산출 (측정 개수 가중치 완전 배제):**
   - 구역 내 특정 카테고리의 측정 지점 개수(예: 출입구 40개 vs 주차장 1개)와 상관없이, 해당 카테고리 내 실측 점수들의 순수 '산술 평균'을 내어 구역 대표 점수(S_i)를 만듭니다.
   - 데이터 개수가 많다고 해서 최종 지수에 더 큰 가중치가 부여되어서는 안 됩니다.

2. **조사되지 않은 카테고리(N/A) 처리:**
   - 특정 구역에 특정 카테고리 실측 데이터가 전혀 없는 경우, **"해당 카테고리가 조사된 전체 구역들의 평균값(Global Mean)"**을 계산하여 대입합니다.
   - 예: 특정 구역에 '주차장(S5)' 데이터가 없다면, 전체 조사 지역 중 주차장 데이터가 존재하는 구역들의 S5 평균값을 해당 구역의 S5 점수로 대체 대입합니다.
   - 이를 통해 모든 구역이 동일하게 5개 카테고리(오각형) 기준을 유지하도록 하여 구역 간 비교 가능성을 확보합니다.


### 7단계 — 구역별 최종 넓이 지수 산출 (오각형 넓이 고정)
- 6단계를 통해 보완된 5개 카테고리 점수(S1, S2, S3, S4, S5)를 사용하여 항상 5개 축 기준의 오각형 순환 넓이 공식에 대입합니다.
  
  Final Index = ( S1·S2 + S2·S3 + S3·S4 + S4·S5 + S5·S1 ) / (5 × 10000) × 100

- 측정되지 않은 카테고리에 전체 평균값을 대입하여 계산했다는 사실을 결과 보고서 각주에 명시합니다.


### 8단계 — 산출물
- xlsx로 결과를 만들 것. 하드코딩된 숫자가 아니라 **실제 수식**으로 작성하고(openpyxl), LibreOffice로 recalc하여 오류 0건을 확인한다.
- 시트 구성 예시:
  1. 카테고리별 개별 지점 원본 데이터 + 세부점수 + Sn 점수 (카테고리마다 시트 분리 권장)
  2. 구역별 Sn 집계표 (평균, 데이터 없는 카테고리는 "N/A" 표기)
  3. 구역별 최종 넓이 지수 (사용된 공식과 가정을 각주로 명시)
- 대화창에는 방법론 요약 + 최종 결과표만 간결하게 제시하고, 세부 데이터는 파일로 전달한다.

## 핵심 유의사항 (요약)
1. **S1~Sn 어떤 것도 임의로 값을 대입하지 않는다.** 데이터가 있으면 반드시 공식으로 계산하고, 없으면 "N/A"로 두고 사용자에게 처리 방식을 확인한다.
2. **카테고리 개수는 고정이 아니다.** PDF에 S6 이상이 추가되면 그 공식을 그대로 파싱하고, 최종 넓이 지수 공식도 PDF 우선으로 확인 후 필요시 순환합산 구조로 일반화한다.
3. 구역 도형(convex hull)이 비정상적으로 크거나 겹치는 등 이상 징후가 있으면 반드시 사용자에게 보고한다.
4. 데이터-공식 변수 매핑(예: 문너비→유효폭)은 항상 근거와 함께 명시한다.

## 핵심 유의사항 (수정)
1. **측정 개수 가중치를 부여하지 마십시오.** 특정 카테고리의 조사 건수가 많더라도 구역 대표점수(S_i)로 개별 단순 평균화되므로 Final Index에 미치는 영향력은 카테고리별로 동일(1/5)합니다.
2. **조사되지 않은 카테고리는 전체 평균으로 대체하여 오각형 형태를 유지합니다.** 카테고리가 누락되었다고 해서 변/축의 개수(n)를 줄여 계산하지 말고, 해당 카테고리의 전체 구역 평균을 대입하여 동일한 5각형 넓이 지수로 비교합니다.

